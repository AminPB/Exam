# جواب‌های آزمون نرم‌افزار

---

## سوال 1 (بارم: 3)

### الف) آزمایش نرم‌افزار از سمت کل سیستم به سمت اجزاء کوچکتر پیش می‌رود یا برعکس؟

**پاسخ:** آزمایش نرم‌افزار از سمت اجزاء کوچکتر به سمت کل سیستم پیش می‌رود (از پایین به بالا). ابتدا واحدهای کوچک تست می‌شوند، سپس یکپارچگی آن‌ها، و در نهایت کل سیستم.

---

### ب) در این ارتباط، چه سطوحی از آزمایش وجود دارد؟

**پاسخ:** سطوح اصلی آزمایش نرم‌افزار عبارتند از:

1. **آزمایش واحد (Unit Testing):** تست کوچکترین اجزای قابل آزمایش (توابع، متدها، کلاس‌ها)
2. **آزمایش یکپارچگی (Integration Testing):** تست تعامل و ارتباط بین واحدها
3. **آزمایش سیستم (System Testing):** تست کل سیستم به صورت یکپارچه
4. **آزمایش پذیرش (Acceptance Testing):** تست نهایی توسط کاربر یا مشتری

---

### پ) در یک سناریو که محصول از نظر فنی عالی است اما کاربران از آن راضی نیستند، وضعیت هر یک از دو عملیات اعتبارسنجی (verification) و صحت سنجی (validation) چگونه است؟

**پاسخ:**

- **Verification (اعتبارسنجی):** **موفق** - چون محصول از نظر فنی عالی است، یعنی محصول را به درستی ساخته‌ایم (Are we building the product right?)

- **Validation (صحت‌سنجی):** **ناموفق** - چون کاربران راضی نیستند، یعنی محصول درست را نساخته‌ایم (Are we building the right product?)

---

### ت) در این سناریو کدام سطح تست احتمالاً ضعیف انجام شده و چرا؟

**پاسخ:** سطح **آزمایش پذیرش (Acceptance Testing)** احتمالاً ضعیف انجام شده است.

**دلیل:** آزمایش پذیرش مسئول بررسی این است که آیا سیستم نیازها و انتظارات کاربر را برآورده می‌کند یا خیر. اگر این مرحله به درستی انجام می‌شد، عدم رضایت کاربران قبل از تحویل نهایی کشف می‌شد.

---

## سوال 2 (بارم: 4)

**اطلاعات گراف:**
- N₀ = {1}
- Nf = {8}
- N = {1, 2, 3, 4, 5, 6, 7, 8}
- E = {(1,2,a), (1,3,b), (1,4,c), (1,5,d), (2,2,e), (2,3,f), (3,7,i), (4,6,j), (5,4,g), (5,5,h), (6,8,l), (7,8,k)}

---

### الف) این گراف CFG را رسم کنید

**پاسخ:**

```
          1 (start)
       /  |  \  \
      a   b  c   d
     /    |   \   \
    2     3    4   5
    |\    |    |   |\
    e i   i    j   g h
    | \   |    |   | |
    2  3  7    6   4 5
       |  |    |
       i  k    l
       |  |    |
       7  8    8
       |      (end)
       k
       |
       8
      (end)
```

**توضیح یال‌ها:**
- از گره 1: به گره‌های 2(a), 3(b), 4(c), 5(d)
- از گره 2: به گره‌های 2(e - حلقه), 3(f)
- از گره 3: به گره 7(i)
- از گره 4: به گره 6(j)
- از گره 5: به گره‌های 4(g), 5(h - حلقه)
- از گره 6: به گره 8(l)
- از گره 7: به گره 8(k)

---

### ب) نیازمندی‌های آزمون (TR) برای پوشش یال (EC) و زوج یال (EPC) + مسیرهای آزمون (TP) برای EC که EPC را پوشش ندهد

**1) نیازمندی‌های آزمون برای پوشش یال (Edge Coverage - EC):**

TR_EC = تمام یال‌های گراف باید حداقل یک بار اجرا شوند:

```
TR_EC = {
  (1,2,a), (1,3,b), (1,4,c), (1,5,d),
  (2,2,e), (2,3,f),
  (3,7,i),
  (4,6,j),
  (5,4,g), (5,5,h),
  (6,8,l),
  (7,8,k)
}
```

تعداد یال‌ها = 12

---

**2) نیازمندی‌های آزمون برای پوشش زوج یال (Edge-Pair Coverage - EPC):**

TR_EPC = تمام زوج یال‌های متوالی (مسیرهای به طول 2):

```
TR_EPC = {
  // از یال‌های خروجی از 1:
  [(1,2,a), (2,2,e)], [(1,2,a), (2,3,f)],
  [(1,3,b), (3,7,i)],
  [(1,4,c), (4,6,j)],
  [(1,5,d), (5,4,g)], [(1,5,d), (5,5,h)],

  // از یال‌های خروجی از 2:
  [(2,2,e), (2,2,e)], [(2,2,e), (2,3,f)],
  [(2,3,f), (3,7,i)],

  // از یال‌های خروجی از 3:
  [(3,7,i), (7,8,k)],

  // از یال‌های خروجی از 4:
  [(4,6,j), (6,8,l)],

  // از یال‌های خروجی از 5:
  [(5,4,g), (4,6,j)],
  [(5,5,h), (5,4,g)], [(5,5,h), (5,5,h)],

  // یال‌های ورودی به 8:
  [(6,8,l)], [(7,8,k)]
}
```

---

**3) مسیرهای آزمون (TP) برای پوشش EC که EPC را پوشش ندهد:**

استراتژی: باید تمام یال‌ها را پوشش دهیم اما از پوشش همه زوج‌های یال اجتناب کنیم.

```
TP = {
  [1, 2, 2, 2, 3, 7, 8],    // پوشش: (1,2,a), (2,2,e), (2,3,f), (3,7,i), (7,8,k)
  [1, 3, 7, 8],              // پوشش: (1,3,b) - این یال قبلاً با زوج (1,3,b)→(3,7,i) پوشش داده شد
  [1, 4, 6, 8],              // پوشش: (1,4,c), (4,6,j), (6,8,l)
  [1, 5, 5, 4, 6, 8]         // پوشش: (1,5,d), (5,5,h), (5,4,g) - (4,6,j) قبلاً پوشش داده شد
}
```

**توضیح:** این مجموعه تمام 12 یال را پوشش می‌دهد اما تمام زوج‌های یال ممکن را پوشش نمی‌دهد. مثلاً زوج [(2,2,e), (2,2,e)] پوشش داده نشده است.

---

### پ) پوشش همه کاربردها (All-Uses Coverage) با فرض متغیر x

**اطلاعات:**
- متغیر: x
- تعریف (def): گره‌های 1 و 3
- کاربرد (use): گره‌های 6 و 7

---

**نیازمندی‌های آزمون (TR) برای All-Uses Coverage:**

باید تمام مسیرهای بدون حلقه (def-clear paths) از هر تعریف به هر کاربرد را پوشش دهیم:

```
TR_all-uses = {
  // از def در گره 1:
  du(1, 6, x): مسیرهایی که از 1 به 6 می‌روند بدون تعریف مجدد x
  du(1, 7, x): مسیرهایی که از 1 به 7 می‌روند بدون تعریف مجدد x

  // از def در گره 3:
  du(3, 6, x): مسیرهایی که از 3 به 6 می‌روند بدون تعریف مجدد x - وجود ندارد
  du(3, 7, x): مسیرهایی که از 3 به 7 می‌روند بدون تعریف مجدد x
}

TR = {
  du(1, 6, x),  // از تعریف در 1 به کاربرد در 6
  du(1, 7, x),  // از تعریف در 1 به کاربرد در 7
  du(3, 7, x)   // از تعریف در 3 به کاربرد در 7
}
```

---

**مسیرهای آزمون (TP) برای برآورده کردن All-Uses Coverage:**

```
TP = {
  // برای du(1, 6, x):
  [1, 4, 6],           // مسیر: 1 --c--> 4 --j--> 6
  [1, 5, 4, 6],        // مسیر: 1 --d--> 5 --g--> 4 --j--> 6

  // برای du(1, 7, x):
  [1, 3, 7],           // مسیر: 1 --b--> 3 --i--> 7
  [1, 2, 3, 7],        // مسیر: 1 --a--> 2 --f--> 3 --i--> 7

  // برای du(3, 7, x):
  [1, 3, 7]            // مسیر: ... --> 3 --i--> 7 (قبلاً پوشش داده شد)
}
```

**مجموعه نهایی مسیرها:**
```
TP = {
  [1, 4, 6, 8],        // برای du(1, 6, x)
  [1, 5, 4, 6, 8],     // برای du(1, 6, x) از طریق 5
  [1, 3, 7, 8],        // برای du(1, 7, x) و du(3, 7, x)
  [1, 2, 3, 7, 8]      // برای du(1, 7, x) از طریق 2
}
```

---

## سوال 3 (بارم: 1)

### محاسبه تعداد حداقل مسیرها برای پوشش یال (M) با استفاده از جبر گراف

**فرض:** حلقه‌ها حداکثر 5 بار تکرار می‌شوند.

**حلقه‌های موجود در گراف:**
- حلقه در گره 2: (2,2,e)
- حلقه در گره 5: (5,5,h)

---

**محاسبه با جبر گراف:**

عبارت مسیر از گره 1 به گره 8:

```
Path(1→8) = Path(1→2→8) + Path(1→3→8) + Path(1→4→8) + Path(1→5→8)
```

**1) مسیر از 1 به 8 از طریق 2:**
```
Path(1→2→8) = (1--a-->2) · (2--e-->2)* · (2--f-->3) · (3--i-->7) · (7--k-->8)
            = a · e* · f · i · k
```

با فرض حداکثر 5 تکرار حلقه e:
- تعداد مسیرها = 6 (0 تا 5 تکرار)

**2) مسیر از 1 به 8 از طریق 3:**
```
Path(1→3→8) = (1--b-->3) · (3--i-->7) · (7--k-->8)
            = b · i · k
```
- تعداد مسیرها = 1

**3) مسیر از 1 به 8 از طریق 4:**
```
Path(1→4→8) = (1--c-->4) · (4--j-->6) · (6--l-->8)
            = c · j · l
```
- تعداد مسیرها = 1

**4) مسیر از 1 به 8 از طریق 5:**
```
Path(1→5→8) = (1--d-->5) · (5--h-->5)* · (5--g-->4) · (4--j-->6) · (6--l-->8)
            = d · h* · g · j · l
```

با فرض حداکثر 5 تکرار حلقه h:
- تعداد مسیرها = 6 (0 تا 5 تکرار)

---

**تعداد کل مسیرها از 1 به 8:**
```
Total = 6 + 1 + 1 + 6 = 14 مسیر
```

---

**تعداد حداقل مسیرها برای پوشش یال (M):**

برای پوشش یال، باید تمام 12 یال را حداقل یک بار اجرا کنیم. با توجه به ساختار گراف:

- یال‌های خروجی از 1: نیاز به حداقل 4 مسیر (برای a, b, c, d)
- یال حلقه در 2: نیاز به 1 مسیر اضافی
- یال حلقه در 5: نیاز به 1 مسیر اضافی

اما می‌توانیم با مسیرهای هوشمندانه‌تر این را کاهش دهیم:

```
مسیر 1: [1, 2, 2, 3, 7, 8]     // پوشش: a, e, f, i, k
مسیر 2: [1, 3, 7, 8]            // پوشش: b (i, k قبلاً پوشش داده شده)
مسیر 3: [1, 4, 6, 8]            // پوشش: c, j, l
مسیر 4: [1, 5, 5, 4, 6, 8]     // پوشش: d, h, g (j, l قبلاً پوشش داده شده)
```

**پاسخ نهایی: M = 4 مسیر**

---

## سوال 4 (بارم: 5)

**گزاره:** P = a ∧ (b ∨ c)

---

### الف) شرایط فعال بودن هر یک از کلازهای a، b و c (Pa, Pb, Pc)

**تعریف:** شرط فعال بودن یک کلاز (clause)، وضعیتی است که در آن تغییر مقدار آن کلاز، مقدار کل گزاره را تغییر می‌دهد.

**Pa (شرط فعال بودن a):**
```
Pa = (b ∨ c) = true
```
زمانی که (b ∨ c) برابر true باشد، تغییر a مقدار P را تغییر می‌دهد.

**Pb (شرط فعال بودن b):**
```
Pb = a ∧ ¬c
```
زمانی که a=true و c=false باشد، تغییر b مقدار P را تغییر می‌دهد.

**Pc (شرط فعال بودن c):**
```
Pc = a ∧ ¬b
```
زمانی که a=true و b=false باشد، تغییر c مقدار P را تغییر می‌دهد.

---

### ب) جدول درستی گزاره P = a ∧ (b ∨ c)

| ردیف | a | b | c | b∨c | P = a∧(b∨c) | Pa | Pb | Pc |
|------|---|---|---|-----|-------------|----|----|-----|
| 1    | 0 | 0 | 0 | 0   | 0           | 0  | 0  | 0   |
| 2    | 0 | 0 | 1 | 1   | 0           | 1  | 0  | 0   |
| 3    | 0 | 1 | 0 | 1   | 0           | 1  | 0  | 0   |
| 4    | 0 | 1 | 1 | 1   | 0           | 1  | 0  | 0   |
| 5    | 1 | 0 | 0 | 0   | 0           | 0  | 0  | 0   |
| 6    | 1 | 0 | 1 | 1   | 1           | 1  | 0  | 1   |
| 7    | 1 | 1 | 0 | 1   | 1           | 1  | 1  | 0   |
| 8    | 1 | 1 | 1 | 1   | 1           | 1  | 0  | 0   |

**توضیح ستون‌ها:**
- **Pa = (b∨c):** ردیف‌های 2, 3, 4, 6, 7, 8 → Pa = 1
- **Pb = a∧¬c:** فقط ردیف 7 → Pb = 1
- **Pc = a∧¬b:** فقط ردیف 6 → Pc = 1

---

### پ) زوج ردیف‌هایی که پوشش GACC را برآورده می‌کنند

**GACC (General Active Clause Coverage):** برای هر کلاز، باید دو تست پیدا کنیم که:
1. شرط فعال بودن کلاز برقرار باشد
2. کلاز یک بار true و یک بار false باشد
3. مقدار گزاره تغییر کند

**برای کلاز a:**
- نیاز: Pa = true (یعنی b∨c = true) و a تغییر کند
- زوج‌های ممکن: (2,6), (2,8), (3,7), (3,8), (4,6), (4,7), (4,8)
- مثال: **(2, 6)** یا **(3, 7)**
  - ردیف 2: a=0, b=0, c=1 → P=0
  - ردیف 6: a=1, b=0, c=1 → P=1

**برای کلاز b:**
- نیاز: Pb = true (یعنی a=1, c=0) و b تغییر کند
- زوج: **(5, 7)**
  - ردیف 5: a=1, b=0, c=0 → P=0
  - ردیف 7: a=1, b=1, c=0 → P=1

**برای کلاز c:**
- نیاز: Pc = true (یعنی a=1, b=0) و c تغییر کند
- زوج: **(5, 6)**
  - ردیف 5: a=1, b=0, c=0 → P=0
  - ردیف 6: a=1, b=0, c=1 → P=1

**TR_GACC:**
```
TR_GACC = {
  (2, 6),  // برای کلاز a
  (5, 7),  // برای کلاز b
  (5, 6)   // برای کلاز c
}
```

یا می‌توان از زوج‌های دیگر نیز استفاده کرد:
```
TR_GACC = {(3, 7), (5, 7), (5, 6)}
```

---

### ت) زوج ردیف‌هایی که پوشش RACC را برآورده می‌کنند

**RACC (Restricted Active Clause Coverage):** مشابه GACC اما با شرط اضافی که تمام کلازهای دیگر ثابت بمانند.

**برای کلاز a:**
- نیاز: b و c ثابت بمانند، a تغییر کند، Pa=true
- زوج‌های ممکن:
  - **(2, 6):** a تغییر (0→1), b=0, c=1 ثابت ✓
  - **(3, 7):** a تغییر (0→1), b=1, c=0 ثابت ✓
  - **(4, 8):** a تغییر (0→1), b=1, c=1 ثابت ✓

**برای کلاز b:**
- نیاز: a و c ثابت بمانند، b تغییر کند، Pb=true
- زوج: **(5, 7):** a=1, c=0 ثابت, b تغییر (0→1) ✓

**برای کلاز c:**
- نیاز: a و b ثابت بمانند، c تغییر کند، Pc=true
- زوج: **(5, 6):** a=1, b=0 ثابت, c تغییر (0→1) ✓

**TR_RACC:**
```
TR_RACC = {
  (2, 6),  // برای کلاز a
  (5, 7),  // برای کلاز b
  (5, 6)   // برای کلاز c
}
```

**نکته:** در این مثال خاص، GACC و RACC یکسان هستند.

---

## سوال 5 (بارم: 3)

### الف) طراحی IDM برای تابع find

```java
int find (File f, String s)
```

این تابع تعداد وقوع رشته s را در فایل f پیدا می‌کند.

---

**1) افراز مبتنی بر واسط (Interface-Based - Q1):**

این افراز بر اساس مشخصات ورودی‌های تابع است:

**برای پارامتر File f:**
```
Q1_f = {
  b1: فایل null است
  b2: فایل خالی است (اندازه = 0)
  b3: فایل حاوی محتوا است (اندازه > 0)
  b4: فایل وجود ندارد
}
```

**برای پارامتر String s:**
```
Q1_s = {
  b1: رشته null است
  b2: رشته خالی است ("")
  b3: رشته یک کاراکتری است
  b4: رشته چند کاراکتری است
}
```

**چرا این افراز صحیح است؟**

✓ **کامل (Complete):** تمام حالات ممکن ورودی‌ها پوشش داده شده است.

✓ **مجزا (Disjoint):** هیچ ورودی در بیش از یک بلوک قرار نمی‌گیرد (مثلاً یک فایل نمی‌تواند هم null و هم خالی باشد).

---

**2) افراز مبتنی بر عملکرد (Functionality-Based - Q2):**

این افراز بر اساس رفتار و نتیجه خروجی تابع است:

```
Q2 = {
  b1: رشته s در فایل f وجود ندارد (خروجی = 0)
  b2: رشته s یک بار در فایل وجود دارد (خروجی = 1)
  b3: رشته s چند بار در فایل وجود دارد (خروجی > 1)
  b4: خطا رخ می‌دهد (فایل قابل خواندن نیست، null pointer، و...)
}
```

**چرا این افراز صحیح است؟**

✓ **کامل (Complete):** تمام نتایج ممکن (0، 1، بیش از 1، خطا) پوشش داده شده است.

✓ **مجزا (Disjoint):** یک اجرای تابع تنها یک نتیجه دارد و در یک بلوک قرار می‌گیرد.

---

### ب) نیازمندی‌های آزمون برای MBCC

**اطلاعات:**
- سه افراز: Q1 با بلوک‌های [a, b, c, d]، Q2 با بلوک‌های [1, 2]، Q3 با بلوک‌های [x, y]
- دو گزینه پایه (Base Choices): (a, 1, x) و (x, 2, b)

**توجه:** گزینه پایه دوم (x, 2, b) نامعتبر است چون x از Q3 است، نه Q1. فرض می‌کنیم منظور (d, 2, x) است.

---

**با فرض گزینه‌های پایه: (a, 1, x) و (d, 2, x):**

**برای گزینه پایه اول: (a, 1, x)**

تمام ترکیبات با تغییر یک پارامتر:
```
1. (a, 1, x)  ← گزینه پایه
2. (b, 1, x)  ← تغییر Q1
3. (c, 1, x)  ← تغییر Q1
4. (d, 1, x)  ← تغییر Q1
5. (a, 2, x)  ← تغییر Q2
6. (a, 1, y)  ← تغییر Q3
```

**برای گزینه پایه دوم: (d, 2, x)**

تمام ترکیبات با تغییر یک پارامتر:
```
7. (d, 2, x)  ← گزینه پایه
8. (a, 2, x)  ← تغییر Q1 (تکراری با #5)
9. (b, 2, x)  ← تغییر Q1
10. (c, 2, x)  ← تغییر Q1
11. (d, 1, x)  ← تغییر Q2 (تکراری با #4)
12. (d, 2, y)  ← تغییر Q3
```

---

**TR_MBCC (حذف تکراری‌ها):**
```
TR_MBCC = {
  (a, 1, x),   // #1 - base choice 1
  (b, 1, x),   // #2
  (c, 1, x),   // #3
  (d, 1, x),   // #4
  (a, 2, x),   // #5
  (a, 1, y),   // #6
  (d, 2, x),   // #7 - base choice 2
  (b, 2, x),   // #9
  (c, 2, x),   // #10
  (d, 2, y)    // #12
}
```

**تعداد کل تست‌ها: 10**

---

**اگر گزینه پایه دوم (x, 2, b) باشد (با فرض x عضو Q1 باشد):**

```
TR_MBCC = {
  (a, 1, x),   // base 1
  (b, 1, x),
  (c, 1, x),
  (d, 1, x),
  (a, 2, x),
  (a, 1, y),
  (x, 2, b),   // base 2
  (a, 2, b),
  (b, 2, b),
  (c, 2, b),
  (d, 2, b),
  (x, 1, b),
  (x, 2, x),
  (x, 2, y)
}
```

---

## پایان جواب‌ها

موفق باشید! 🎓
